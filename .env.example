# Google AI Studio API key (for Gemini agent + image generation)
GEMINI_API_KEY=your-api-key-here

# Supabase PostgreSQL connection URL (for session persistence)
DATABASE_URL=postgresql://user:password@host:6543/postgres?pgbouncer=true

# Directory where journal entries are saved
JOURNAL_DIR=./journals

# Server port
PORT=8000
